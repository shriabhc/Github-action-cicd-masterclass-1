# name of the workflow
name: Code

# when the workflow should run
on:
  push:

jobs:
  code-checkout:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        uses: action/checkout@v7

      - name: print the message
        run: echo "echo code clone"
        
